{
  "name": "@rolldown/test-dev-server-tests",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "pnpm run test:legacy && pnpm run test:browser",
    "test:legacy": "vitest run --config=vitest.config.mts",
    "test:browser": "vitest run --config=vitest.config.browser.mts"
  },
  "dependencies": {
    "@rolldown/test-dev-server": "workspace:*",
    "@types/kill-port": "^2.0.3",
    "execa": "^9.2.0",
    "fast-glob": "catalog:",
    "kill-port": "^2.0.1",
    "rimraf": "^6.0.1",
    "vitest": "catalog:"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "playwright": "^1.48.0"
  }
}
